---
title: Git 常用命令速查指南
date: 2026-02-19 21:25:00
tags: [Git, 版本控制, 教程]
categories: 技术
---

# Git 常用命令速查指南

Git 是现代软件开发中最流行的版本控制系统。本文整理了最常用的 Git 命令及其使用场景，帮助你快速上手。

## 一、仓库初始化与克隆

### git init - 初始化新仓库

当你开始一个新项目，需要创建版本控制时使用：

```bash
# 在当前目录初始化 Git 仓库
git init

# 创建指定名称的目录并初始化
git init my-project
```

**使用场景：** 开始本地新项目时

### git clone - 克隆远程仓库

从远程仓库复制完整代码到本地：

```bash
# 克隆公开仓库
git clone https://github.com/kkwalking/kkwalking.github.io.git

# 克隆并指定本地目录名
git clone https://github.com/user/repo.git my-folder
```

**使用场景：** 获取他人代码、fork 项目后同步、开始参与开源项目

---

## 二、日常开发核心命令

### git add - 添加文件到暂存区

将修改的文件标记为准备提交：

```bash
# 添加单个文件
git add index.html

# 添加指定目录
git add src/

# 添加所有修改（包括新文件）
git add .

# 添加已跟踪文件的修改（不包含新文件）
git add -u
```

**使用场景：** 准备提交前，确认要纳入版本的文件

### git commit - 提交变更

将暂存区的内容保存到仓库历史：

```bash
# 提交并填写说明
git commit -m "修复登录按钮样式问题"

# 提交并打开编辑器写详细说明
git commit

# 修改上一次提交（仅修改说明）
git commit --amend
```

**使用场景：** 完成一个功能点、修复一个 bug、进行一次代码重构

**💡 最佳实践：** 提交说明要清晰，说明"做了什么"而非"改了什么"

### git push - 推送到远程

将本地提交推送到远程仓库：

```bash
# 推送到默认分支（main/master）
git push

# 推送到指定分支
git push origin feature/login

# 首次推送分支并设置上游
git push --set-upstream origin feature/login

# 强制推送（谨慎使用！）
git push --force
```

**使用场景：** 代码完成、需要协作、准备部署

### git pull - 拉取远程更新

获取远程最新代码并合并到本地：

```bash
# 拉取当前分支
git pull

# 从指定分支拉取
git pull origin main

# 拉取并变基（保持历史整洁）
git pull --rebase
```

**使用场景：** 开始工作前、合并他人代码、准备推送前

---

## 三、查看状态与历史

### git status - 查看仓库状态

了解当前文件变更情况：

```bash
# 查看完整状态
git status

# 简洁模式
git status -s
```

**输出含义：**
- `A` - 新增文件
- `M` - 修改文件
- `D` - 删除文件
- `??` - 未跟踪文件

### git log - 查看提交历史

查看项目变更历程：

```bash
# 查看完整历史
git log

# 简洁展示（每行一条）
git log --oneline

# 图形化展示分支
git log --graph --oneline

# 查看最近 N 条
git log -5

# 查看特定文件的变更历史
git log index.html
```

### git diff - 查看具体变更

详细查看代码修改内容：

```bash
# 查看工作区 vs 暂存区
git diff

# 查看暂存区 vs 最新提交
git diff --cached

# 查看工作区 vs 最新提交
git diff HEAD

# 查看两个版本的差异
git diff v1.0.0 v2.0.0
```

---

## 四、分支管理

### git branch - 分支操作

```bash
# 列出所有本地分支
git branch

# 列出所有远程分支
git branch -r

# 列出本地+远程分支
git branch -a

# 创建新分支
git branch feature/new-login

# 创建并切换到新分支
git checkout -b feature/new-login

# 删除分支
git branch -d feature/old

# 强制删除（未合并的分支）
git branch -D feature/old
```

### git checkout - 切换分支

```bash
# 切换到已有分支
git checkout main

# 切换到上一分支
git checkout -

# 创建并切换（新语法）
git switch -c feature/new
```

**使用场景：** 开发新功能、修复 bug、切换环境

### git merge - 合并分支

将一个分支的变更合并到当前分支：

```bash
# 合并指定分支到当前分支
git merge feature/login

# 合并并保留合并历史
git merge --no-ff feature/login

# 取消合并
git merge --abort
```

**使用场景：** 功能完成合并到主分支、修复 bug 后合并回主分支

---

## 五、撤销与回退

### git restore - 撤销文件修改

```bash
# 撤销工作区修改（恢复到暂存区状态）
git restore index.html

# 撤销所有工作区修改
git restore .

# 恢复已删除的文件
git restore --source=HEAD~1 deleted-file.txt
```

### git reset - 回退版本

```bash
# 软回退（保留修改在暂存区）
git reset --soft HEAD~1

# 混合回退（保留修改在工作区）
git reset HEAD~1

# 硬回退（丢弃所有修改）
git reset --hard HEAD~1

# 回退到指定版本
git reset --hard abc123
```

**⚠️ 注意：** `--hard` 会丢失修改，谨慎使用！

### git revert - 反向提交

创建一个新提交来抵消之前的提交：

```bash
# 反转上一次提交
git revert HEAD

# 反转指定提交
git revert abc123
```

**使用场景：** 需要保留历史但撤销某次提交时

---

## 六、暂存工作

### git stash - 临时保存工作区

当需要切换分支但不想提交时：

```bash
# 暂存当前工作
git stash

# 暂存并添加说明
git stash save "修复到一半的 bug"

# 查看所有暂存
git stash list

# 恢复最近一次暂存
git stash pop

# 恢复指定暂存
git stash pop stash@{0}

# 删除暂存
git stash drop
```

**使用场景：** 紧急修复 bug、切换分支、临时处理其他任务

---

## 七、远程仓库管理

### git remote - 远程仓库操作

```bash
# 查看远程仓库
git remote -v

# 添加远程仓库
git remote add origin https://github.com/user/repo.git

# 修改远程仓库地址
git remote set-url origin https://github.com/user/new-repo.git

# 删除远程仓库
git remote remove origin
```

### git fetch - 获取远程数据

获取远程最新数据但不合并：

```bash
# 获取所有远程分支
git fetch

# 获取指定分支
git fetch origin main

# 获取所有远程分支（包括 tags）
git fetch --all
```

---

## 八、标签管理

### git tag - 标签操作

```bash
# 查看所有标签
git tag

# 创建轻量标签
git tag v1.0.0

# 创建附注标签（推荐）
git tag -a v1.0.0 -m "Version 1.0.0"

# 查看标签信息
git show v1.0.0

# 删除标签
git tag -d v1.0.0

# 推送标签到远程
git push origin v1.0.0

# 推送所有标签
git push origin --tags
```

**使用场景：** 版本发布、标记重要节点

---

## 💡 实用技巧

### 1. 配置别名

```bash
# 配置常用命令别名
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.last 'log -1 HEAD'
```

### 2. 忽略文件

创建 `.gitignore` 文件：

```gitignore
# 忽略所有 .log 文件
*.log

# 忽略 node_modules 目录
node_modules/

# 忽略构建目录
dist/
build/

# 忽略本地配置文件
.env.local
```

### 3. 查看文件谁写的

```bash
# 查看文件每行最后修改人
git blame index.html

# 查看文件指定行范围
git blame -L 10,20 index.html
```

---

## 📚 常见问题

### Q: 提交说明写错了怎么办？
```bash
git commit --amend -m "正确的提交说明"
```

### Q: 忘记添加文件了怎么办？
```bash
git add forgotten-file.js
git commit --amend --no-edit
```

### Q: 推送被拒绝怎么办？
```bash
# 先拉取远程最新
git pull

# 如果有冲突，解决后重新提交
# 然后再推送
git push
```

### Q: 想回到某个历史版本看看？
```bash
# 先查看历史找到 commit hash
git log --oneline

# 创建新分支指向那个版本
git checkout -b temp-branch abc123
```

---

## 🎯 快速参考表

| 命令 | 用途 | 频率 |
|------|------|------|
| `git status` | 查看状态 | ⭐⭐⭐ |
| `git add .` | 添加修改 | ⭐⭐⭐ |
| `git commit -m "说明"` | 提交 | ⭐⭐⭐ |
| `git push` | 推送 | ⭐⭐⭐ |
| `git pull` | 拉取 | ⭐⭐⭐ |
| `git log --oneline` | 查看历史 | ⭐⭐ |
| `git branch` | 分支操作 | ⭐⭐ |
| `git checkout -b` | 创建并切换分支 | ⭐⭐ |
| `git merge` | 合并分支 | ⭐ |
| `git stash` | 暂存工作 | ⭐ |

---

## 🔗 推荐资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 交互式学习](https://learngitbranching.js.org/)
- [Git 速查表](https://cheatography.com/)

---

**Happy Coding!** 🚀
